<div class="jumbotron">
<% @bookings.each do |booking| %>
<% if booking.toilet.user == current_user %>
    <% n = 0 %>
    <% n = n + 1 %>
    <div class="card mb-3" style="max-width: 700px;">
      <div class="row mx-2 my-2">
         <div class="col-8">
            <h5 class="card-title">Toilet N°   <%= booking.toilet_id %></h5>
            <p class="card-text">Arrival time:    <%= booking.arrivaltime %></p>
            <p class="card-text">What is it for?  <%= booking.pooptype%></p>
            <p class="card-text">Created at:   <%= booking.created_at %></p>
          </div>
          <div class="col-4">
            <p><%= link_to 'Edit the booking', new_toilet_booking_path(booking), :class => "btn btn-secondary" %></p>
            <p><%= link_to 'Delete the booking', booking, :class => "btn btn-secondary", method: :delete, data: { confirm: 'Are you sure?' } %></p>
          </div>
      </div>
    </div>
    <% else %>
  <% end %>
 <h1 class="display-4"> You earned <%= n %>  🧻 !</h1>
  <% end %>
</div>
